## ۴) مقایسه جایگاه‌به‌جایگاه با سکوهای مرجع

> برای هر سکو: ابتدا «الگوی قابل‌تقليد» آن، سپس «وضعیت نخشا در برابر همان الگو»، و در پایان کلیدشکاف‌ها. امتیازها در بخش ۵ جمع‌بندی شده‌اند. نماد ✓ = موجود، ✗ = غایب، ± = ناقص.

### ۴-۱ Shopify Admin (الگوی UX عملیاتی و افزونه‌پذیری)

- **الگوی قابل‌تقليد:** جدول‌های غنی با bulk action، جزئیات قابل‌تعریف، صفحات فعالیت/تحلیل آماده، اپ-استور (اکوسیستم افزونه)، «Command Palette» و صفحه‌ی Git-installed app ثبت عمودی به‌همراه گزارش‌های خودکار.
- **وضعیت نخشا:** جدول/فهرست‌ها ساده هستند؛ بدون bulk action واقعی، بدون صفحه‌ی گزارش‌گیری، بدون فیلتر ترکیبی ذخیره‌شده، بدون آلمان‌گری سراسری.
- **کلیدشکاف‌ها:** ✗ گزارش‌ مالی/فروش؛ ✗ bulk select/شده‌های دسته‌جمعی؛ ✗ جست‌وجوی global؛ ± فرم‌ها (جادوگر ۷ مرحله‌ای خوب است اما بدون validation زنده‌ی پیشرفته).

### ۴-۲ Stripe Dashboard (الگوی BI و جریان پول)

- **الگوی قابل‌تقليد:** «Overview» شامل KPIهای بلاک + نمودار سری‌زمانی تعاملی + فیلتر بازه‌ی زمانی؛ گزارش‌گیری CSV؛ بقیه‌ی توضیحات در یک چیدمان سلسله‌مراتبی؛ «export» یک‌کلیکه؛ نقش نمایشً اصلی همیشه قابل‌دیدن.
- **وضعیت نخشا:** ✗ هیچ صفحه‌ی BI نیست؛ ✗ هیچ نموداری نیست (بدون Recharts حتی)؛ ✗ هیچ خروجی CSV/Excel نیست؛ ✗ داشبورد «Overview» کارεται ندارد.
- **کلیدشکاف‌ها:** این بزرگ‌ترین شکاف محصولی است. حتی یک «خلاصه‌ی وضعیت» برای ادمین وجود ندارد؛ داده‌ها پراکنده‌اند.

### ۴-۳ AWS Console / CloudWatch (الگوی Observability و امنیت)

- **الگوی قابل‌تقليد:** تک‌پنل سراسری، نقش‌های IAM دقیق، مورد ۲FA/MFA اجباری، CloudTrail (ممیزی) و CloudWatch (metric+alert) به‌صورت پیش‌فرض روی همه‌چیز.
- **وضعیت نخشا:** Sentry بدون DSN (متفاوت است: هیچ خطایی ضبط نمی‌شود)؛ ✗ بدون metric؛ ✗ بدون alert؛ ✗ بدون لاگ مرکزشده قابل-پرس‌وجو؛ ✗ بدون ممیزی از فعالیت admin؛ ✗ بدون RBAC با granularity.
- **کلیدشکاف‌ها:** `backend/services` و `server.js` هیچ محل هُک برای metric/alert ندارند؛ «چه کسی چه کاری در داشبورد کرده» قابل ردیابی نیست. این برای یک داشبورد ادمین، ریسک امنیتی-عملیاتی است.

### ۴-۴ Vercel / Netlify Dashboard (الگوی توسعه و مشاهده‌ی پیش‌نمایش)

- **الگوی قابل‌تقليد:** دیپلوی پیش‌نمایش per-PR، رول‌بک یک‌کلیکه، محیط‌های متعدد با env مجزا، ساعت‌ها و log زنده در مرورگر، وضعیت «deploy» اول در top.
- **وضعیت نخشا:** staging روی Render دارد؛ ✗ بدون پیش‌نمایش per-PR؛ ✗ بدون رول‌بک خودکار؛ ✗ بدون log viewer متمرکز در داشبورد؛ ± CI فقط بررسی‌کننده است نه منتشرکننده.
- **کلیدشکاف‌ها:** تعمیر انتشار — CI در حالت «verify-only» است؛ Deploy و نمای کلاس‌بندی نشده است.

### ۴-۵ Firebase / Supabase Console (الگوی مقیاس و آپلود)

- **الگوی قابل‌تقليد:** جدول‌های داده با فیله‌تر/سورت/صفحه بعدی سمت کلاینت، Authّ built-in با مدیریت کاربر، صفحه‌ی Storage با preview و سیاست‌های امکان، نسخه‌بندی و audit log پروژه.
- **وضعیت نخشا:** آپلود دارد (multer+sharp) اما ✗ مدیریت رسانه (با جستجو/پیش‌نمایش/bulk) ندارد؛ ✗ مدیریت کاربران فعال (حذف/غیرفعال/نقش) ندارد؛ ± ایندکس‌های Mongo در bootstrap وجود دارد اما با pix مرگmic ure مشخص نیست.
- **کلیدشکاف‌ها:** صفحه‌ی «محتوای رسانه» و «کاربران» در داشبورد بسیار ابتدایی است؛ هیچ سرویس‌داده-مصرفی سمت کلاینت نیست.

### ۴-۶ Grafana (الگوی داشبورد داده-محور)

- **الگوی قابل‌تقليد:** پنل‌های قابل‌چیدمان، datasource چندگانه، alert rule، variable (فیلتر دینامیک)، export/import dashboard JSON.
- **وضعیت نخشا:** نداشتن حتی یک «پنل» — داده به‌صورت جدول دیده می‌شود نه به‌صورت سری‌زمانی/توزیع.
- **کلیدشکاف‌ها:** نیاز به یک لایه‌ی «متاداده‌ی تجمیعی» در بک‌اند (aggregate endpoints) و یک لایه‌ی نمودار/فیلتر در فرانت؛ فعلاً صفر است.

### ۴-۷ GitHub (الگوی فرایند و ممیزی)

- **الگوی قابل‌تقليد:** PR review workflow، branch protection، محتوای «Audit log» برای فعالیت ادمین، فهرست درخواست‌های دسترسی، خروجی CSV از logها.
- **وضعیت نخشا:** قابلیت همکاری خوب در CI دارد ولی ✗ لاگ فعالیت قابل خروجی برای ادمین ندارد؛ ✗ صفحه‌ی «چه کسی چه تغییری داد» ندارد.
- **کلیدشکاف‌ها:** برای یک داشبورد مدیریت محتوا، «ممیزی» حداقل باید شامل نسخه‌ی قبلی و کنش خالق باشد؛ حاضر نیست.

### ۴-۸ پنل فروشندگان دیجی‌کالا (الگوی پنل ایرانی پیشرفته)

- **الگوی قابل‌تقليد:** داشبورد عملکرد فروش (نمودار، KPI، مقایسه‌ی بازه)، مدیریت سفارش با اقدام انبوه، مدیریت کالا با گردش و وضعیت، مدیریت دعوا/پرداخت.
- **وضعیت نخشا:** مفهوم‌سطحی: ⛌ KPI/نمودار؛ ✗ اقدام انبوه؛ ± فرم‌ها خوب ولی بدون گردش وضعیت کامل (workflow engine).
- **کلیدشکاف‌ها:** پنل دیجی‌کالا ثابت می‌کند که در بازار ایران هم «داشبوردِ تصمیم» (نه فقط CRUD) توقع است؛ نخشا فاقد متاداده‌ی تجمیعی برای فروش/بازدید است.

### ۴-۹ پنل اسنپ‌کار/تپسی (شرکا و رانندگان)

- **الگوی قابل‌تقليد:** به‌روزرسانی زنده (websocket)، حالت «در حال ماموریت» با نقشه و شمایل بلادرنگ، اعلان‌های حرکت، نمودار درآمد روزانه، گزارش‌گیری CSV.
- **وضعیت نخشا:** ✗ بلادرنگ (همه poll)؛ ✗ نسخه‌ی «وضعیت زنده» برای آگهی/تور/پست وجود ندارد؛ ✗ اعلان push/in-app نیست.
- **کلیدشکاف‌ها:** نقشه فقط نمایش استاتیک دارد؛ برای داشبورد «شهری» (ویت‌های متحرک؟) پتانسیل زنده‌سازی کاملاً بلااستفاده مانده است.

### ۴-۱۰ پنل توسعه‌دهندگان کافه‌بازار

- **الگوی قابل‌تقليد:** داشبورد نمایه‌ی اپ (install/uninstall, crash, rating) با جدول‌های قابل‌فیلتر و نمودار روند؛ سگ‌این نشست (API key) با سطح دسترسی؛ مستندسازی API.
- **وضعیت نخشا:** ✗ API key/توکن با scope؛ ✗ نمودار روند؛ ✗ داشبورد «سلامت سرویس» برای ادمین.
- **کلیدشکاف‌ها:** پشتیبانی از swagger هست ولی هیچ سطح «کلید API احرازشده با scope» نیست؛ برای داشبورد B2B ضروری است.

### ۴-۱۱ پنل زرین‌پال / پی‌پینگ (گیت‌وی و تراکنش)

- **الگوی قابل‌تقليد:** داشبورد تراکنش (فیلتر بازه، دسته‌بندی، جستجو، گزارش CSV)، نمودار درآمد، وزر گرفتن + دستی بازگشت وجه، مدیریت کاربر محدود.
- **وضعیت نخشا:** ✗ هیچ زیرساخت تراکنش/پرداخت وجود ندارد؛ ✗ هیچ نمودار درآمد نیست.
- **کلیدشکاف‌ها:** (در صورتی که محصول نهایی بخواهد درآمد/VOD داشته باشد) فقدان کامل لایه‌ی مالی.

### ۴-۱۲ پنل ملی‌پیامک (MeliPayamak — سرویسی که نخشا هم از آن استفاده می‌کند)

- **الگوی قابل‌تقليد:** داشبورد ارسال/دریافت پیامک، گزارش با فیلتر زمانی، دفترچه تلفن/گروه‌ها، اعتبار و صورت‌حساب، تبلیغ به‌صورت چند-خط. نقطه‌ی جالب: سرویس‌دهنده‌ی خارجی که نخشا به آن وابسته است، خودش گزارش و Credit بصری دارد.
- **وضعیت نخشا:** فقط «ارسال OTP»؛ ✗ هیچ صفحه‌ی گزارش ارسال/مصرف اعتبار نیست؛ ✗ هیچ نمایش موجودی/شارژ نیست؛ ✗ هیچ fallback (مثلاً پیامک جایگزین/صف ارسال) نیست.
- **کلیدشکاف‌ها:** وابستگی تک‌نقطه‌ای به یک سرویس SMS بی‌آنکه داشبورد حتی «وضعیت آن» را نشان دهد یا مواظب اعتبار باشد.

### ۴-۱۳ پنل تبلیغ‌دهندگان دیوار

- **الگوی قابل‌تقليد:** مدیریت تعداد آگهی، آمار بازدید/تماس (نمودار)، ارتقا و پیکربندی بسته‌ها، گزارش بازدهی.
- **وضعیت نخشا:** ✗ آمار بازدید/تعامل روی هر محتوا وجود ندارد؛ ✗ امکان ارتقای آگهی (featured) وجود ندارد؛ ✗ نظارت بر «سلامت محتوا» وجود ندارد.
- **کلیدشکاف‌ها:** حلقه‌ی «اثر → شمارش → نمایش به ادمین» که در هر پنل ایرانی هست، در نخشا نیست.

---

## ۵) ماتریس امتیازدهی (نگاه ترکیبی)

امتیاز ۱-۱۰ برای هر سکو در ۴ ستون کلیدی و امتیاز کلی (وزن‌دار از ۹ معیار بخش ۲-۲):

| سکو | امنیت | BI/داده | UX | عملیات/پایش | کلی (از ۱۰) | پردازش کلی |
|---|---|---|---|---|---|---|
| Stripe | 10 | 10 | 9 | 10 | **9.4** | مرجع BI |
| Shopify | 9 | 9 | 10 | 9 | **9.2** | مرجع عملیات‌پذیری |
| AWS/CW | 10 | 8 | 7 | 10 | **8.7** | مرجع امنیت/پایش |
| Vercel | 8 | 7 | 9 | 9 | **8.2** | مرجع DX |
| Grafana | 9 | 10 | 8 | 10 | **9.0** | مرجع visualization |
| Firebase / Supabase | 9 | 8 | 8 | 8 | **8.0** | مرجع داده‌ی کلاینت |
| GitHub | 10 | 7 | 9 | 9 | **8.4** | مرجع ممیزی |
| پنل دیجی‌کالا | 7 | 8 | 8 | 7 | **7.4** | قوی‌ترین ایرانی |
| اسنپ/تپسی | 7 | 8 | 8 | 8 | **7.6** | بلادرنگ |
| کافه‌بازار | 8 | 7 | 7 | 7 | **7.2** | API-سنتریک |
| زرین‌پال/پی‌پینگ | 8 | 8 | 7 | 7 | **7.4** | مالی |
| ملی‌پیامک | 6 | 7 | 6 | 6 | **6.2** | سرویس‌گرا |
| دیوار | 7 | 7 | 7 | 7 | **6.9** | بازدهی محتوا |
| **نخشا (فعلی)** | **6.5** | **2.0** | **6.5** | **3.0** | **≈ 4.4** | — |

> خوانش: در ستون‌های «امنیت/UX» نخشا در ۲/۳ پایین جدول قرار دارد اما به مرجع های داخلی نزدیک است؛ در «BI و عملیات» از همه‌ی مراجع (حتی ملی‌پیامک) عقب‌تر است — این دو ستون بیش‌ترین وزن را هم دارند.

---